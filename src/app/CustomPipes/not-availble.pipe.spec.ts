import { NotAvailblePipe } from './not-availble.pipe';

describe('NotAvailblePipe', () => {
  it('create an instance', () => {
    const pipe = new NotAvailblePipe();
    expect(pipe).toBeTruthy();
  });
});
